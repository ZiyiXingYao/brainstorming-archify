# 架构设计文档模板（固定结构，按此填写）

> 本模板对应产物 `specs/design/01-架构设计.md`，**全项目唯一一份**。
> 生成时逐节遵循本模板：**按此结构填写**，章节顺序不变。
> 不适用的章节保留标题，并写"不适用：<原因>"。
> 章节名默认使用本模板的中文名；当对话语言非中文时可对应翻译，但结构顺序不变。
> 表格可增补列，但不得删减既有列。

> **增量维护（重要）**：本文件是全局总纲，后续讨论若只涉及某一个功能模块，**不要重写本文件**。
> 落盘前先读盘，再判决本次讨论是否影响架构——判据以技能的 Incremental Persist Protocol 为准，
> 至少包括：新增、删除或改名模块，模块的设计状态翻转，类换了所属模块，新增或删除类，
> 文件树变化，模块间依赖变化，跨模块接口变化，接口状态翻牌，全局流程的模块链变化。
> 凡是受影响的条目**只改那几条**，未受影响的章节**逐字保留**（含原有措辞、顺序、格式），禁止顺手润色。
> 除受影响的条目外，增量更新时只额外刷新元信息区的「最近更新」一行。
> 本次结论与现有内容冲突时，不静默覆盖：先列出差异，交由用户裁决。

---

# <项目名> 架构设计

> **状态**：草稿 / 已确认 —— 新建时写"草稿"；经用户审阅确认后改"已确认"；其后增量更新不再退回"草稿"
> **最近更新**：YYYY-MM-DD —— 新建时填当天；每次增量更新刷新为当天。同一次落盘若同时更新多份文档，这几份填**同一天**
> **参与**：<用户>、<AI>
> **来源**：头脑风暴讨论沉淀（最近一次讨论主题：<主题>）

---

## 1. 背景与问题

<为什么做这件事。当前是什么状态，痛在哪里，什么场景会触发它。
用叙述写，不要列需求条目。>

## 2. 目标与非目标

**目标**
- <这次要达到什么，尽量可验证>

**非目标**
- <明确这次不做什么，避免范围蔓延>

## 3. 方案对比与选型

<列出 2-3 个候选方案，说明各自思路、优点、缺点和最终结论。>

| 方案 | 思路 | 优点 | 缺点 | 结论 |
|------|------|------|------|------|
| A（推荐） | ... | ... | ... | 采用 |
| B | ... | ... | ... | 未采用：<原因> |

## 4. 整体功能说明

<叙述整个系统对外提供哪些能力、这些能力串起来解决什么问题。
读者只看这一节能建立系统的功能全景。不要在此展开模块内部细节。>

## 5. 功能点需求说明

<把系统对外提供的功能逐条说清楚——这是全项目功能点的唯一锚点。
本清单**必须完备**：系统有的功能点一个都不能漏，没有的功能点一个都不能编。

每个功能点用一句叙述说明「谁在什么场景下要它、它要达成什么」。
它是一个描述性需求条目，**不是**「需求编号 + 验收条件」那种规格条目——**禁止 Given/When/Then**。

「参与模块」写实现该功能点会碰到的模块（对应第 6.2 节模块清单），
「对应流程」写它由第 10 节的哪条（或哪几条）流程串起来落地。
每条功能点都必须指向至少一条流程；反过来，第 10 节登记的流程集合必须覆盖本表每一个功能点，
两边对不上就是缺陷。>

| 功能点 | 需求说明 | 参与模块 | 对应流程 |
|--------|----------|----------|----------|
| <功能点> | <一句话说清要达成什么> | <模块A>、<模块B> | <流程名> |

## 6. 功能模块划分

### 6.1 划分依据与粒度

<说明这套模块划分是怎么来的：按什么维度切（职责、生命周期、数据归属、变更频率……），
每个切口的接缝落在哪里，为什么落在这里而不是别处。
列出考虑过但被否决的划分方式，并写明各自被否决的原因——避免后续讨论反复推翻。

划分粒度：一个模块应当能独立回答"它做什么、怎么用、依赖谁"，并且能被一次讨论讲透。
一个模块的类详细设计一屏放不下、或需要拆成多次讨论才能讲完 → 该拆细；
两个模块总是同时改动、彼此之间找不到清晰的接缝 → 该合并。>

### 6.2 模块清单

<每个模块一行。模块名即该模块设计文档的文件名主体，两处必须一致。
**模块名在全项目内唯一，不得重名**——它同时是文件名主体和所有交叉引用的锚点。
尚未设计的模块也要登记，序号按当前最大序号往后预留，「对应文档」写 `（待创建）`。>

| 模块 | 职责 | 边界（做什么 / 不做什么） | 对应文档 | 设计状态 |
|------|------|--------------------------|----------|----------|
| <通信模块> | <一句话职责> | <做什么；不做什么> | `specs/design/02-<通信模块>.md` | 已设计 |
| <存储模块> | <一句话职责> | <做什么；不做什么> | `specs/design/03-<存储模块>.md`（待创建） | 未设计 |

### 6.3 模块依赖关系

<只讲依赖方向、依赖强度（强依赖 / 可选依赖）与是否存在循环依赖。
具体依赖了哪些类 / 函数 / 变量，不在这里展开——那是第 7 节接口矩阵的职责。
可用文字或 mermaid 图。>

## 7. 跨模块接口矩阵

<全局索引与缺口追踪表，**只做索引，不重复细节**。
签名的唯一权威是**调用方模块文档第 4 节**——本表摘录必须与其逐字一致；
两者出现分歧时以调用方模块文档为准，并把它修正到本表。
每有一对跨模块依赖就有一行。新增模块或接口变化时必须同步本表。>

| 调用方模块 | 提供方模块 | 依赖摘要（类 / 期望签名） | 状态 | 详见 |
|-----------|-----------|------------------------|------|------|
| <A> | <B> | 类 `<X>`；`<f>(<a>: <T>) -> <R>`；变量 `<v>` | 已落地 / 待提供 / 有差异 | `specs/design/0N-<A>.md` 第 4 节 |

**状态枚举**

- **已落地**：双方都已设计，且所提供的类/函数/变量在硬约束上与调用方的期望一致（由提供方落盘时翻牌）
- **待提供**：提供方模块尚未设计，本条是对它的需求输入
- **有差异**：双方都已设计，但硬约束对不上（函数名、入参、返回类型或边界语义不一致），差异待裁决

**填表规则**

**状态只在本表维护。** 模块文档第 4 节只写需求与签名，不写状态——状态会因别人的落盘而变化，
放在一处就不会出现两处对不上。本表的状态分三段收口：

1. **调用方产生** —— 调用方模块落盘时写入本条，状态 `待提供`，并在变更清单中提示用户后续单独讨论提供方模块
2. **提供方翻牌** —— 提供方模块落盘前读盘时看到指向自己的 `待提供` 条目，视为对它的输入需求，设计时**必须逐条处理**：
   硬约束一致 → 改 `已落地`；硬约束不一致 → 改 `有差异` 并列出差异。
   软约束（参数具体类型、错误类型体系、内部结构）由提供方自行决定，不影响状态
3. **裁决者收口** —— `有差异` 由用户裁决后，按裁决结果改调用方或提供方的文档，两边对齐后状态改 `已落地`；
   若任一方暂时无法修改，保持 `有差异`，并在两边的「待定问题与风险」各留一条说明

任何情况下都不得把提供方已经处理过的条目留在 `待提供`——那意味着这个缺口没人认领。

第 1、2 段只动本表，不碰任何模块文档。第 3 段需要改模块文档时，**用户裁决本身就是授权**，
可以改；没有裁决就不得擅自改动对方的模块文档。

## 8. 核心类设计

### 8.1 核心类清单

<本表只登记**串联整体功能的核心类**，不是全项目类清单——全项目每个类的完整归属由各模块文档
第 2.1 节的类清单承载。定义文件写相对项目根的路径。

**核心类判定标准（可机械核对）**：出现下列任一情形的类才算核心类——
出现在第 10 节全局功能流程某条流程的模块级调用链上，或作为第 7 节跨模块接口矩阵某行的接口承载者。
反过来，只在某一个模块内部使用、不出现在第 10 节任何流程的模块级调用链上、也不是第 7 节任何一行的
接口承载者的类，**不得**出现在本表。某个模块没有核心类是合法结果，不必为凑齐而登记。

**与模块文档的子集关系**：本表必须是各模块文档第 2.1 节类清单之并集的**子集**——
每个模块文档的第 2.1 节是该模块完整类清单的权威。本表出现、而任何模块类清单中都没有的类，是缺陷；
同一个类同时出现在两个模块的类清单里，同样是缺陷。>

| 类名 | 所属模块 | 职责 | 定义文件 |
|------|---------|------|---------|
| `<ClassName>` | <模块名> | <一句话职责> | `<path/to/file.ext>` |

### 8.2 类结构关系

<**只讲核心类之间**的继承、组合、聚合、依赖关系，并说明每条关系为何存在。用文字或 mermaid 图表达，
重点说明跨模块的类关系。
类的**调用**关系不在这里写——那属于 8.3 类调用关系，两节不重叠。>

### 8.3 类调用关系

<登记**核心类之间**的调用关系（谁调用谁）。本节每一条关系的两端都必须是在 8.1 核心类清单中
列出的核心类。用表格或 mermaid 图表达，风格与模板其余部分一致。
本节只记录方向级的、类到类的调用，不要求与第 7 节接口矩阵逐行对应——接口矩阵是接口级视角，是另一件事。>

| 调用方类 | 被调用类 | 调用说明 |
|---------|---------|---------|
| `<ClassName>` | `<ClassName>` | <一句话说明这次调用做什么> |

> 架构文档不描述模块内部的函数级调用链，也不给出类的字段表或函数表——这些只出现在对应的模块文档。

## 9. 代码文件树组织

<项目落地后的目录与文件组织，**只到「模块文件夹 + 核心文件」**，不要求文件级完备。
读者从这里应能看清代码按模块怎么分目录、核心类放在哪里。

**必须能定位 8.1 全部核心类的定义文件**：树里必须能查到第 8.1 节核心类清单中**每一个**核心类的定义文件；
某个核心类的定义文件在树里找不到，就是缺陷。
反过来，**不要求树覆盖全部源文件**：非核心类所在的源文件可以不出现在树里，这不算缺陷——
全项目每个类的完整清单及各自的定义文件由各模块文档第 2.1 节的类清单承载。

树中**出现的**每个目录和每个源文件都要在下面的说明表中交代职责。不含构建产物、依赖目录。

模块与目录**不要求一一对应**：一个模块可以分散在多个目录，一个目录也可以容纳多个模块。
当某个目录明确归属某个模块时，在说明表里点明；对不上时不要强行凑成一一对应。>

```
<project-root>/
├── src/
│   ├── <模块目录>/
│   │   └── <核心类定义文件>
│   └── <另一个模块目录>/
│       └── <核心类定义文件>
└── <入口文件>
```

| 路径 | 职责 |
|------|------|
| `src/<模块目录>/` | <这个目录放什么，归属哪个模块> |
| `src/<模块目录>/<核心类定义文件>` | <这个文件定义哪个核心类> |
| `<入口文件>` | <程序入口> |

## 10. 全局功能流程

<只画**方向级骨架**（模块级调用链 + 每跳承担的核心类 + 每跳数据形态），不写模块内部细节——模块内的函数级调用链写在各自模块文档的第 5 节。
每条流程讲清六件事：服务功能点（这条流程服务于第 5 节的哪个或哪些功能点）
→ 触发入口（外部请求 / 上游调用 / 定时任务 / 事件订阅）
→ 模块级调用链（哪个模块在跨边界处调用了哪个接口，每一跳由哪个核心类承担）
→ 每跳的数据形态（模块之间传的是什么结构）
→ 结果落地（整条流程跑完最终产出什么、落到哪里、返回给谁）
→ 失败处理（哪一跳会失败、失败后整条流程如何收场：补偿 / 回滚 / 降级 / 直接上报，
错误最终由谁感知）。模块内部的失败细节归各模块文档第 5 节，这里只写跨模块层面的收场方式。

**服务功能点**：本节每条流程都必须写明它服务于第 5 节的哪些功能点，与第 5 节「对应流程」列
**两个方向都要对齐**——第 5 节每个功能点至少被本节一条流程服务；反过来，本节每条流程至少服务一个功能点，
两边对不上就是缺陷。

**全部功能流程都要登记**：系统的每一条功能流程都必须出现在本节，**包括模块级调用链只涉及一个模块的**——
单模块功能流程同样要登记、同样只写方向级，不得因为不跨模块就省略。

**只写方向级**：本节不描述模块内部的函数级调用链，只写模块级调用链以及每一跳承担的核心类；
函数级细节归各模块文档的端到端业务流程（第 5 节）。

流程名的对齐关系：本节登记的每条**功能流程**都要在参与模块文档的第 5 节里出现且同名，
跨边界跳在参与模块文档第 5 节里按同一流程名展开到函数级；反过来，纯属模块内部**实现**细节
（不是功能流程）的流程只写在模块文档，不要求登记到本节。
多条流程按下面骨架复制。>

### 10.N <流程名>

**服务功能点**

<这条流程服务于第 5 节的哪个或哪些功能点；与第 5 节「对应流程」列双向对齐。>

**触发入口**

<具体触发点：谁调用、什么条件触发。>

**模块级调用链**

<例：`<模块A>` →(接口 `<f>`，由核心类 `<ClassA>` 承担)→ `<模块B>` →(接口 `<g>`，由核心类 `<ClassB>` 承担)→ `<模块C>`。>

> 若流程只在一个模块内完成，调用链只有一跳，仍按本节格式登记，不展开该模块内部函数调用。

| 步骤 | 所在模块 | 跨边界调用的接口 | 输入数据形态 | 输出数据形态 | 承担的核心类 |
|------|---------|----------------|-------------|-------------|-------------|
| 1 | <模块A> | — | <结构 / 字段 / 类型> | <结构 / 字段 / 类型> | `<ClassA>` |
| 2 | <模块B> | `<f>`，由 <模块A> 调用 | <结构 / 字段 / 类型> | <结构 / 字段 / 类型> | `<ClassB>` |

**结果落地**

<整条流程最终产出什么，落到哪里（返回值给谁 / 写入哪个存储 / 推给哪个下游）。>

**失败处理**

<逐条列出会失败的跳点与收场方式：哪一步失败 → 已完成的步骤怎么处理（补偿 / 回滚 / 保持）
→ 是否降级 → 错误最终上报给谁。只写跨模块层面，模块内部的失败细节不在这里展开。>

## 11. 关键决策记录

<架构层面的取舍。模块内部的决策写在各自的模块文档里，不要放这里。>

| 决策点 | 选择 | 理由 | 被否决的选项 |
|--------|------|------|--------------|
| ... | ... | ... | ... |

## 12. 待定问题与风险

<待定项解决后、风险解除后，可以删掉对应条目——删条目属于「受影响条目」，不需要额外授权。>

- **待定**：<还没想清楚的问题> —— 为什么未定：<...>
- **风险**：<可能出问题的地方> —— 影响：<...>
