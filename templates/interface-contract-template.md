# 接口契约模板（固定结构，按此填写）

> 本模板对应产物 `specs/design/00-接口契约.md`，**全项目唯一一份**；文件名固定为 `00-接口契约.md`——`00-` 是**固定字面前缀**，**不参与模块序号序列**（架构总纲固定 `01-`、各模块文档为 `02+`），**不随模块增删改变**，**不受「序号追加不重排」管辖**，**永不重编号**。
> 生成时逐节遵循本模板：**按此结构填写**，章节顺序不变。
> 全文二级标题共 **11** 个：`## 总则`（**不编号**）+ `## 1.` 至 `## 10.`（带号节共 **10** 个）。总则节不编号，其后各节从 1 起顺序编号，三级小节编号跟随所属节（如 `### 1.1`、`### 6.2`、`### 8.1`）。三级小节中**第 1–7 节合计 21 个**（七类各 `x.1` / `x.2` / `x.3` 三段）；第 8 节另有 `### 8.1` / `### 8.2` 两个小节，**不计入这 21 个**。
> 不适用的**章节与三级小节**保留标题，并写"不适用：<原因>"；七类接口节（第 1–7 节）的 `x.1` / `x.2` / `x.3` 三段缺任一段即判定为缺陷；某一段确实不适用时，同样保留该小节标题并写"不适用：<原因>"（如 `x.3` 写"不适用：沿用第 9 节跨类别共同约定"）。
> 章节名默认使用本模板的中文名；当对话语言非中文时可对应翻译，但结构顺序不变。
> 表格可增补列，但不得删减既有列。

> **增量维护（重要）**：本文件是**受管持久文档**；接口或数据模型的新增、删除、改名，都在**同一次落盘**内同步到对应节（`x.1` 清单表行、`x.2` 逐项契约小节、可追溯列、代码侧落地物映射表一并改到位），不留与正文不一致的登记。
> 更新时机与本文件是否受影响**不另立判据**，一律沿用技能的 `## Incremental Persist Protocol`；凡受影响的条目只改那几条，未受影响的章节**逐字保留**（含原有措辞、顺序、格式）。

> **本文件不配图**：本模板产出物**不配 SVG 图**，其所在目录**不需要** `diagrams/` 子目录；自审与审查 **MUST NOT** 因本文件没有配图而判定缺陷。

**可追溯列口径速查（`x.1` 清单表强制列）**

下表是七类接口 `x.1` 清单表**可追溯列**的口径，按接口类型选取；**以本表为准**——某族只需一侧时，以那一侧即为完整（动态库 API 只需「调用方」、TCP 只需「对端」，**不**因此缺归属方列）。列名不得统一改成"写入方"这类与类型不贴合的写法。

| 接口类型 | 可追溯列（按本表口径；只需一侧的族以那一侧即为完整） |
|----------|----------------------------------------------------|
| MySQL 数据表 | 数据维护方 + 本服务侧的读写权限 |
| Redis key 与结构 | 写入方 + 读取方 |
| MQ 消息通道 | 方向（完整链路两端）+ 分片键 |
| HTTP 接口 | 提供方 + 调用方 |
| gRPC 接口 | 提供方 + 调用方 |
| 动态库 API | 调用方 |
| TCP 接口 | 对端 |

---

# <项目名> 接口契约

> **状态**：草稿 / 已确认 —— 新建时写"草稿"；经用户审阅确认后改"已确认"；其后增量更新不再退回"草稿"
> **最近更新**：YYYY-MM-DD —— 新建时填当天；每次增量更新刷新为当天。同一次落盘若同时更新多份文档，这几份填**同一天**
> **参与**：<用户>、<AI>
> **来源**：头脑风暴讨论沉淀（最近一次讨论主题：<主题>）
> **对应架构**：`specs/design/01-架构设计.md`（架构总纲）—— 本文件跨全部模块，指向总纲而非某一模块文档

---

## 总则

<本节是全文的第一个检查点：读者在进入任何一类接口之前，就应知道"这份文档说了算、它落地在哪些文件、它不管什么"。
本节含**权威源声明**、**适用范围与形态**、**代码侧落地物映射表**三部分，缺任一部分即判定为缺陷。总则节**不编号**。>

**权威源声明**

<必须明确写出：本文件是全部对外接口与数据模型的**唯一权威源**；与代码侧落地物出现分歧时**以本文件为准**，
并把代码侧落地物记为待修正项。不得用"仅供参考""以代码为准"这类反向表述。>

**适用范围与形态**

<写明两件事，缺一不可：
① **覆盖范围与形态**：本服务的部署形态（单进程 / 多进程 / 多服务）、通道形态（如只有一条通道形态还是多条，以及是否存在另一套命名口径）；
② **不覆盖什么**：必须显式列出本文件**不管**的内容，其中**必须含**一条——「**进程内跨模块接口不写在本文件**」，
进程内跨模块接口登记在 `specs/design/01-架构设计.md` 第 7 节跨模块接口矩阵与调用方模块文档第 4 节。

只写"覆盖什么"而不写"不覆盖什么"，判定为缺陷；不覆盖项里缺「进程内跨模块接口不写在本文件」这一条，同样判定为缺陷。>

**代码侧落地物映射表**

<代码侧落地物与本文件覆盖章节的对应关系：读者据此知道本文件落地在哪些文件里、哪儿对不上要看本文件。
某类接口确实没有代码侧落地物时，在「代码侧落地物」列标注「无」，并在「说明」列写清原因。>

| 代码侧落地物 | 覆盖章节 | 说明 |
|--------------|----------|------|
| `<path/to/file>` | 第 N 节 <节名> | <为什么是它；或标"无"时的原因> |

## 1. MySQL 数据表

<本节写本服务在 MySQL 中的表与字段契约。粒度：一张表一行进 `1.1` 清单表，一张表一个小节进 `1.2` 逐项契约，字段级到列。
只登记本服务读取或写入的 MySQL 表；其它存储（Redis、MQ 等）各归其节。
本类接口不适用时（本服务完全不碰 MySQL），保留标题写"不适用：<原因>"。>

### 1.1 清单表

<一行一张表，**必须含「编号」「名称」「用途」与可追溯列**。
MySQL 的可追溯列口径为**数据维护方**（谁写入 / 维护这张表）与**本服务侧的读写权限**（本服务对这张表是只读还是可读写）；
口径不得替换成统一叫"写入方"。缺可追溯列或某行留空，判定为缺陷。>

| 编号 | 名称 | 用途 | 数据维护方 | 本服务侧的读写权限 |
|------|------|------|-----------|-------------------|
| 1 | `<table_name>` | <这张表存什么、被哪条流程用到> | <数据源方 / 本服务 / 其他服务> | <只读 / 读写 / 不写> |

### 1.2 逐项契约

<逐表展开字段级契约，一张表一个小节。
**表格是权威**：字段表 MUST 至少给出「名称」「类型或长度」「语义」三类信息；MySQL 可按需再加「约束 / 默认值」「枚举取值 / 映射」等列。
原始片段（DDL）只作**附证**，贴在字段表之后；表格与片段不一致时**以表格为准**，并把片段修正为与表格一致。
无 DDL 片段的表不因缺少附证判缺陷，但字段表仍须给出上述三类信息。下表是单张表的填写骨架，按表复制。>

#### `<table_name>`

| 字段名 | 类型或长度 | 语义 | 约束 / 默认值 |
|--------|-----------|------|--------------|
| `<column>` | `<TYPE(n)>` | <这个字段存什么、边界在哪> | <非空 / 默认值 / 取值范围> |

附证片段（可选）：

```sql
-- <按需贴入本表 DDL 片段作为附证；与上方字段表冲突时以上方字段表为准，并修正本片段>
```

### 1.3 该类规则

<本类的命名与维度规则、可靠性 / 幂等 / 重试口径、版本与兼容策略。
MySQL 一般写：表名与列名命名规则、引擎 / 字符集 / 主键约定、字段类型取值口径、DDL 变更与版本兼容策略。
确无追加规则时，保留本小节标题并写"不适用：沿用第 9 节跨类别共同约定"——引用跨类别共性约定一律写「第 9 节」。>

| 规则 | 说明 |
|------|------|
| <命名规则 / 维度规则> | <…> |
| <版本与兼容> | <…> |

## 2. Redis key 与结构

<本节写本服务在 Redis 中的 key 与 value 结构契约。粒度：一个 key（或一类 key 模式）一行进 `2.1` 清单表，一个 key 一个小节进 `2.2` 逐项契约，结构到 field / value 字段级。
本服务不适用 Redis 时，保留标题写"不适用：<原因>"。>

### 2.1 清单表

<一行一个 key（或一类 key 模式，占位符用 `{…}` 表示），**必须含「编号」「名称」「用途」与可追溯列**。
Redis 的可追溯列口径为**写入方**与**读取方**；某行未写明该 key 由谁写入，判定为缺陷。>

| 编号 | 名称（key 模式） | 用途 | 写入方 | 读取方 |
|------|-----------------|------|--------|--------|
| 1 | `<prefix:{entity}:…>` | <这个 key 存什么、被谁用> | <本服务 / 其他服务 / 暂不写> | <本服务 / 外部> |

<「写入方」写"暂不写"时，须在 `2.2` 或 `2.3` 说明原因（例如保留登记只为契约完整性）。>

### 2.2 逐项契约

<逐个 key 展开 value 结构，一个 key 一个小节。
**表格是权威**：字段表 MUST 至少给出「名称」「类型或长度」「语义」三类信息；Redis 可按需再加「TTL」「取值 / 枚举」等列。
原始片段只作**附证**（可贴 key 常量登记片段或 value JSON 结构示例）；无附证片段不判缺陷。下表是单个 key 的填写骨架，按 key 复制。>

#### `<key 模式>`

| 字段 | 类型或长度 | 语义 |
|------|-----------|------|
| `<field / json 字段>` | `<Type>` | <这个字段存什么、边界在哪> |

附证片段（可选）：

```text
<按需贴入 key 常量片段或 value JSON 结构示例；与上方字段表冲突时以上方字段表为准>
```

### 2.3 该类规则

<本类的命名与维度规则、可靠性 / 幂等 / 重试口径、版本与兼容策略。
Redis 一般写：key 命名格式与占位符含义、类型选型（HASH / SET / STRING）、TTL 与续期口径、状态外化与启动恢复（读回）口径。
确无追加规则时，保留本小节标题并写"不适用：沿用第 9 节跨类别共同约定"。>

| 规则 | 说明 |
|------|------|
| <命名格式 / 占位符含义> | <…> |
| <TTL 与恢复口径> | <…> |

## 3. MQ 消息通道

<本节写本服务在消息队列上的 topic 与载荷契约。粒度：一个 topic 一行进 `3.1` 清单表，一个 topic 一个小节进 `3.2` 逐项契约，载荷结构到字段级。
只登记本服务参与收发（含仅发、仅收）的 topic。本服务不适用 MQ 时，保留标题写"不适用：<原因>"。>

### 3.1 清单表

<一行一个 topic，**必须含「编号」「名称（topic）」「用途」与可追溯列**。
MQ 的可追溯列口径为**方向**与**分片键**：
「方向」写**完整链路两端**（如 `A→B`），不得只写 topic 名与载荷；
「分片键」写 topic 末段占位符（同一实体的消息落同一分区、保序处理）。
某行只写了 topic 名与载荷、没有写明消息方向，判定为缺陷。>

| 编号 | 名称（topic） | 用途 | 方向（完整链路两端） | 分片键 |
|------|--------------|------|---------------------|--------|
| 1 | `<topic.{entity}>` | <这条消息承载什么、被谁用> | <上游→本服务 / 本服务→下游> | `<{entity}>` |

<「分片键」为全局 topic（不带占位符）时写"—"，并在 `3.3` 说明。>

### 3.2 逐项契约

<逐个 topic 展开载荷结构，一个 topic 一个小节。
**表格是权威**：字段表 MUST 至少给出「名称」「类型或长度」「语义」三类信息；可按需再加「必填 / 可选」「枚举取值 / 映射」等列。
原始片段（proto message）只作**附证**，贴在字段表之后；表格与片段不一致时**以表格为准**，并把片段修正为与表格一致。
无 proto 片段的 topic 不因缺少附证判缺陷。下表是单个 topic 的填写骨架，按 topic 复制。>

#### `<topic>`（载荷 `<MessageName>`）

| 字段 | 类型或长度 | 语义 |
|------|-----------|------|
| `<field>` | `<Type>` | <这个字段存什么、边界在哪> |

附证片段（可选）：

```proto
// <按需贴入该载荷 proto message 片段作为附证；与上方字段表冲突时以上方字段表为准>
```

### 3.3 该类规则

<本类的命名与维度规则、可靠性 / 幂等 / 重试口径、版本与兼容策略。
MQ 一般写：topic 命名格式与占位符含义、可靠 / 可丢分级与死信口径、幂等与去重键、序列化格式与版本兼容。
确无追加规则时，保留本小节标题并写"不适用：沿用第 9 节跨类别共同约定"。>

| 规则 | 说明 |
|------|------|
| <命名格式 / 维度规则> | <…> |
| <可靠 / 可丢与死信> | <…> |
| <幂等与去重键> | <…> |

## 4. HTTP 接口

<本节写本服务对外提供的 HTTP 接口契约（本服务作为提供方）与所依赖的上游 HTTP 接口（本服务作为调用方）。
粒度：一个接口一行进 `4.1` 清单表，一个接口一个小节进 `4.2` 逐项契约，请求参数与响应字段到字段级。
本服务不适用 HTTP 时，保留标题写"不适用：<原因>"。>

### 4.1 清单表

<一行一个接口，**必须含「编号」「名称」「用途」与可追溯列**。
HTTP 的可追溯列口径为**提供方**与**调用方**；「名称」写 方法 + 路径（如 `GET /v1/xxx`）。>

| 编号 | 名称（方法 + 路径） | 用途 | 提供方 | 调用方 |
|------|--------------------|------|--------|--------|
| 1 | `<GET /v1/path>` | <这个接口解决什么、被谁用> | <本服务 / 上游> | <…> |

### 4.2 逐项契约

<逐个接口展开请求参数与响应字段，一个接口一个小节。
**表格是权威**：字段表 MUST 至少给出「名称」「类型或长度」「语义」三类信息；可按需再加「位置（path / query / header / body）」「必填 / 可选」「枚举取值」等列。
原始片段只作**附证**（可贴请求 / 响应示例）；无附证片段不判缺陷。下表是单个接口的填写骨架，按接口复制。>

#### `<METHOD> <path>`

请求参数：

| 名称 | 类型或长度 | 语义 | 位置 | 必填 |
|------|-----------|------|------|------|
| `<name>` | `<Type>` | <…> | <path / query / header / body> | <是 / 否> |

响应字段：

| 名称 | 类型或长度 | 语义 |
|------|-----------|------|
| `<name>` | `<Type>` | <…> |

附证片段（可选）：

```http
<按需贴入请求 / 响应示例作为附证；与上方字段表冲突时以上方字段表为准>
```

### 4.3 该类规则

<本类的命名与维度规则、可靠性 / 幂等 / 重试口径、版本与兼容策略。
HTTP 一般写：路径与版本命名、鉴权与鉴权头、错误码与响应体统一结构、超时与重试、幂等键。
确无追加规则时，保留本小节标题并写"不适用：沿用第 9 节跨类别共同约定"。>

| 规则 | 说明 |
|------|------|
| <路径与版本命名> | <…> |
| <错误码与响应体结构> | <…> |
| <超时与重试> | <…> |

## 5. gRPC 接口

<本节写本服务的 gRPC 接口契约。粒度：一个方法一行进 `5.1` 清单表，一个方法一个小节进 `5.2` 逐项契约，请求 / 响应 message 到字段级。
本服务不适用 gRPC 时，保留标题写"不适用：<原因>"。>

### 5.1 清单表

<一行一个方法，**必须含「编号」「名称」「用途」与可追溯列**。
gRPC 的可追溯列口径为**提供方**与**调用方**；「名称」写 `Service.Method`。>

| 编号 | 名称（Service.Method） | 用途 | 提供方 | 调用方 |
|------|-----------------------|------|--------|--------|
| 1 | `<Service.Method>` | <这个方法解决什么、被谁用> | <本服务 / 上游> | <…> |

### 5.2 逐项契约

<逐个方法展开请求 / 响应 message，一个方法一个小节。
**表格是权威**：字段表 MUST 至少给出「名称」「类型或长度」「语义」三类信息；可按需再加「必填 / 可选」「枚举取值 / 映射」等列。
原始片段（proto 的 service 与 message）只作**附证**，贴在字段表之后；表格与片段不一致时**以表格为准**，并把片段修正为与表格一致。
下表是单个方法的填写骨架，按方法复制。>

#### `<Service>.<Method>`

请求（`<RequestMessage>`）：

| 字段 | 类型或长度 | 语义 |
|------|-----------|------|
| `<field>` | `<Type>` | <…> |

响应（`<ResponseMessage>`）：

| 字段 | 类型或长度 | 语义 |
|------|-----------|------|
| `<field>` | `<Type>` | <…> |

附证片段（可选）：

```proto
// <按需贴入 service 与 message 片段作为附证；与上方字段表冲突时以上方字段表为准>
```

### 5.3 该类规则

<本类的命名与维度规则、可靠性 / 幂等 / 重试口径、版本与兼容策略。
gRPC 一般写：service / method 命名、proto package 与版本兼容、状态码映射、超时与重试、一元 / 流式选型。
确无追加规则时，保留本小节标题并写"不适用：沿用第 9 节跨类别共同约定"。>

| 规则 | 说明 |
|------|------|
| <命名与 package> | <…> |
| <版本兼容> | <…> |
| <状态码与超时> | <…> |

## 6. 动态库 API

<本节写本服务对外提供或依赖的动态库（so / dll / dylib）API 契约。粒度：一个函数（或导出接口）一行进 `6.1` 清单表，一个函数一个小节进 `6.2` 逐项契约，签名与参数 / 返回值到字段级。
本服务不产出、也不依赖任何动态库时，保留标题写"不适用：<原因>"。>

### 6.1 清单表

<一行一个导出函数（或接口），**必须含「编号」「名称」「用途」与可追溯列**。
动态库 API 的可追溯列口径为**调用方**（谁会加载并调用这个动态库）。>

| 编号 | 名称（函数 / 接口名） | 用途 | 调用方 |
|------|----------------------|------|--------|
| 1 | `<func_name>` | <这个函数提供什么、被谁调> | <调用方进程 / 模块> |

### 6.2 逐项契约

<逐个函数展开签名、参数与返回值，一个函数一个小节。
**表格是权威**：字段表 MUST 至少给出「名称」「类型或长度」「语义」三类信息；可拆成"参数表"与"返回值表"两张。
原始片段（头文件签名）只作**附证**，贴在字段表之后；表格与片段不一致时**以表格为准**，并把片段修正为与表格一致。
下表是单个函数的填写骨架，按函数复制。>

#### `<func_name>`

参数与返回值：

| 名称 | 类型或长度 | 语义 |
|------|-----------|------|
| `<param>` | `<Type>` | <含义、方向（入 / 出 / 出入）、约束> |
| `返回值` | `<RetType>` | <返回值含义与边界（失败时返回什么）> |

附证片段（可选）：

```c
// <按需贴入该函数的头文件声明作为附证；与上方字段表冲突时以上方字段表为准>
```

### 6.3 该类规则

<本类的命名与维度规则、可靠性 / 幂等 / 重试口径、版本与兼容策略。
动态库一般写：函数命名与前缀、ABI 与版本兼容、所有权与内存归属（谁分配谁释放）、线程安全与可重入。
确无追加规则时，保留本小节标题并写"不适用：沿用第 9 节跨类别共同约定"。>

| 规则 | 说明 |
|------|------|
| <命名与前缀> | <…> |
| <ABI 与版本兼容> | <…> |
| <所有权 / 线程安全> | <…> |

## 7. TCP 接口

<本节写本服务的 TCP 接口契约（私有协议 / 长连接）。粒度：一个接口（协议）一行进 `7.1` 清单表，一个接口一个小节进 `7.2` 逐项契约，报文帧格式到字段级。
本服务不适用 TCP 时，保留标题写"不适用：<原因>"。>

### 7.1 清单表

<一行一个接口（协议），**必须含「编号」「名称」「用途」与可追溯列**。
TCP 的可追溯列口径为**对端**（连接另一端的对端方）。>

| 编号 | 名称（接口 / 协议名） | 用途 | 对端 |
|------|----------------------|------|------|
| 1 | `<协议名>` | <这条 TCP 通道承载什么、被谁用> | <对端服务 / 设备> |

### 7.2 逐项契约

<逐个接口展开报文帧格式，一个接口一个小节。
**表格是权威**：报文帧格式表同样 MUST 给出「名称」「类型或长度」「语义」三类信息（字节数与编码写进「类型或长度」列）；可按需再加「取值 / 枚举」「备注」等列。
TCP 私有协议以帧格式表承载契约、天然没有代码形态的原始片段时，**不因缺少附证片段判缺陷**；有报文帧示例时作**附证**贴在其后，冲突以表格为准。
下表是单个接口的填写骨架，按接口复制。>

#### `<接口 / 协议名>`

| 字段 | 类型或长度（字节数 / 编码） | 语义 |
|------|----------------------------|------|
| `<field>` | `<uint8 / 2 字节 BE …>` | <含义与边界> |

附证片段（可选）：

```text
<按需贴入报文帧示例作为附证；与上方帧格式表冲突时以上方帧格式表为准>
```

### 7.3 该类规则

<本类的命名与维度规则、可靠性 / 幂等 / 重试口径、版本与兼容策略。
TCP 一般写：帧头 / 帧尾与长度字段、字节序、编解码、心跳与超时重连、粘包 / 分包处理、最大帧长。
确无追加规则时，保留本小节标题并写"不适用：沿用第 9 节跨类别共同约定"。>

| 规则 | 说明 |
|------|------|
| <帧结构 / 字节序> | <…> |
| <心跳与重连> | <…> |
| <粘包 / 分包与最大帧长> | <…> |

## 8. 其他对外接口

<兜底节：收纳**不属于第 1–7 节七类**的对外接口，例如对外文件（格式与路径约定）、CLI 子命令、第三方回调、进程间命名管道等。
两条红线：**不得**把不属于七类任一类的接口硬塞进第 1–7 节（例如把 CLI 子命令写进 HTTP 接口节，判定为缺陷）；也**不得**漏记已知的对外接口。
本节为空时，保留标题并写"不适用：本项目无七类之外的对外接口"，不判定为缺陷。

本节不是七类接口节，不强制三段式；但登记形态与七类节一致：先给清单表（含可追溯列，按该接口的实际归属选取「归属方 / 使用方」两列），再逐项契约（表格权威，附证片段可选）。>

### 8.1 清单表

| 编号 | 名称 | 用途 | 归属方 | 使用方 |
|------|------|------|--------|--------|
| 1 | <文件格式 / 子命令 / 回调名> | <这个接口解决什么、被谁用> | <定义方 / 提供方> | <使用方> |

### 8.2 逐项契约

<逐个接口展开，形态与七类节的 `x.2` 一致：**表格是权威**，字段表 MUST 至少给出「名称」「类型或长度」「语义」三类信息；附证片段可选（有则贴在表格之后，冲突以表格为准）。>

#### `<接口名>`

| 名称 | 类型或长度 | 语义 |
|------|-----------|------|
| `<name>` | `<Type>` | <…> |

## 9. 跨类别共同约定

<本节收录**跨类别共有**的约定，供第 1–7 节各类的 `x.3` 以"沿用第 9 节跨类别共同约定"引用——因此引用一律写「第 9 节」，不得用未编号的"共同约定节"之类指代。
本节只写真正跨类别复用的约定：幂等与去重键、错误码体系、超时与重试、版本比对口径、时间与时区、命名风格、字符编码等。
只在本类成立、不能跨类复用的规则写回各类的 `x.3`，不要堆在这里。
某项约定确不存在时，保留本表并写"不适用：<原因>"，不删节。>

| 约定 | 口径 |
|------|------|
| <幂等与去重键> | <…> |
| <错误码体系> | <…> |
| <超时与重试> | <…> |
| <版本比对口径> | <…> |

## 10. 待定问题与风险

<待定项解决后、风险解除后，可以删掉对应条目。属于某类接口的待定项与风险也统一记在这里，不散落到各类接口节。>

- **待定**：<还没想清楚的问题> —— 为什么未定：<...>
- **风险**：<可能出问题的地方> —— 影响：<...>

---

> **使用说明（填写总则）**：本文件是**骨架构 + 填写说明**，不是样例库——`<…>` 是占位与提示，落盘时替换为真实内容，**不得把说明段原样留在产物里**。
> 落盘前自检三条：① 全文二级标题共 **11** 个（`## 总则` 不编号 + `## 1.` 至 `## 10.` 带号节 10 个）；② **第 1–7 节合计 21 个**三级小节（第 1–7 节每节都有 `x.1` / `x.2` / `x.3` 三段；第 8 节的 `### 8.1` / `### 8.2` 不计入这 21 个）；③ 每类 `x.1` 清单表都含该类型对应的**可追溯列**（口径见文首速查表），无行留空。
> 本文件**不配图**：不引用 SVG，不需要 `diagrams/` 目录；审查与自审不得因无图判缺陷。
