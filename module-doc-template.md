# 功能模块设计文档模板（固定结构，按此填写）

> 本模板对应产物 `specs/design/<序号>-<模块名>.md`，**每个功能模块一份**。
> 生成时逐节遵循本模板：**按此结构填写**，章节顺序不变。
> 不适用的章节保留标题，并写"不适用：<原因>"（例如纯数据结构类没有函数，第 3 节的函数表写"不适用：<原因>"）。
> 章节名默认使用本模板的中文名；当对话语言非中文时可对应翻译，但结构顺序不变。
> 表格可增补列，但不得删减既有列。

> **落盘前必做**：先列 `specs/design/` 现有文件并读 `01-架构设计.md`（第 6.2 节模块清单、第 8.1 节核心类清单、第 9 节代码文件树、第 7 节跨模块接口矩阵、第 10 节全局功能流程）。
> 本模块文档第 2.1 节的类清单是本模块**完整**类清单的唯一权威；架构总纲第 8.1 节的核心类清单必须是**各已设计模块**第 2.1 节类清单之并集的**子集**（属于尚未设计模块的核心类在架构总纲第 8.1 节临时登记，待该模块落盘时收进本表并与架构总纲对齐）；
> 架构总纲第 7 节中指向本模块的"待提供"条目，本次设计必须**逐条处理并翻牌**（一致→已落地，不一致→有差异并列出差异）；
> 本文档第 5 节中在架构总纲第 10 节登记的**每条功能流程**——**跨模块的**与**只在本模块内完成的**都算——流程名必须与架构总纲第 10 节中同名流程一致。

> **内容形态**：混合式——职责、边界、类关系、业务流程用叙述；字段、函数、依赖用表格。

---

# <模块名> 设计

> **状态**：草稿 / 已确认 —— 新建时写"草稿"；经用户审阅确认后改"已确认"；其后增量更新不再退回"草稿"
> **最近更新**：YYYY-MM-DD —— 新建时填当天；每次增量更新刷新为当天。同一次落盘若同时更新多份文档，这几份填**同一天**
> **参与**：<用户>、<AI>
> **来源**：头脑风暴讨论沉淀（最近一次讨论主题：<主题>）
> **对应架构**：`specs/design/01-架构设计.md` 第 6.2 节「模块清单」

---

## 1. 模块职责与边界

### 1.1 职责与边界

<这个模块是干什么的，一句话定位，然后展开。
必须写清四件事：做什么、不做什么、依赖谁、被谁依赖。
"不做什么"尤其重要——它划定了与其他模块的接缝，避免职责蔓延。>

### 1.2 本模块参与的功能点

<列出本模块参与的功能点，以及本模块在每个功能点中承担的职责。
「功能点」列的名称必须与架构总纲第 5 节「功能点需求说明」中的功能点名称一致；
「对应流程」列的名称必须与本文档第 5 节的流程、以及架构总纲第 10 节的流程同名。

双向核对：架构总纲第 5 节登记、却没有任何模块在本子节认领的功能点是缺陷；
本子节列出、但架构总纲第 5 节不存在的功能点同样是缺陷。
本子节与第 5 节各流程的「服务功能点」标注必须相互一致——本模块在某个功能点上承担的职责，
必须能在第 5 节对应流程的「服务功能点」里找到；反过来，第 5 节每条流程的服务功能点，
也必须在本子节里有对应行。>

| 功能点 | 本模块承担的职责 | 对应流程 |
|--------|----------------|----------|
| <功能点名> | <本模块在这个功能点中承担什么，边界到哪> | <流程名> |

## 2. 本模块类清单与关系

### 2.1 类清单

| 类名 | 职责 | 定义文件 |
|------|------|---------|
| `<ClassName>` | <一句话职责> | `<path/to/file.ext>` |

<本表是本模块**完整**类清单的唯一权威——本模块的每一个类都必须列在这里，一个都不能漏，并写清各自的定义文件。
第 3 节「类详细设计」必须与本表一一对应。

本表与架构总纲第 8.1 节之间是**子集**关系，不是相等：架构总纲第 8.1 节只登记串联整体功能的**核心类**，
它必须是**各已设计模块**第 2.1 节类清单之并集的**子集**。属于某个**已设计模块**的核心类，若在该模块第 2.1 节
类清单中找不到，是缺陷；同一个类同时出现在两个模块的类清单里，同样是缺陷。
属于**尚未设计模块**的核心类，因该模块第 2.1 节尚不存在，允许暂只在架构总纲第 8.1 节临时登记；本次落盘把
该模块设计出来时（设计状态翻「已设计」、去掉 `（待创建）`），必须把它收进本表并与架构总纲第 8.1 节对齐，
按落盘协议 Rule 4 与 Rule 5 收口。

非核心类的定义文件**不必**出现在架构总纲第 9 节的代码文件树里——第 9 节只承载模块文件夹与核心文件，
这是预期结果而非缺陷；但架构总纲第 9 节的树必须能定位第 8.1 节每一个核心类的定义文件。>

### 2.2 类关系

<本模块内类之间的继承、组合、聚合、依赖、调用关系；以及跨模块的类关系。
说明每条关系的性质与建立原因。可用文字或 mermaid 图。>

## 3. 类详细设计

<逐个类展开，**每个类都不允许遗漏**（第 2.1 节列出的类必须在这里全部出现）。
每个类按下面的三件套写：类职责叙述 → 字段表 → 函数表。
下面的骨架是对单个类的填写格式，按类复制。

新增的类若插在中间，**后面各类的小节编号要按顺序改号**——编号是位置标记，出现两个 `3.2` 会让引用失准。>

### 3.N `<ClassName>`

<这个类为什么存在。它承担什么职责，为什么这些职责归它而不归别的类，
它的生命周期由谁管理，它在什么场景下被创建和使用。>

**字段表**

<列出该类的全部实例字段与类字段。字段名用代码标识符原样书写。>

| 字段名 | 类型 | 可见性 | 作用 | 约束 / 默认值 |
|--------|------|--------|------|--------------|
| `<field>` | `<Type>` | `public` / `protected` / `private` | <这个字段存什么、被谁读写、为什么需要它> | <取值范围、是否可空、默认值、是否线程安全> |

**函数表**

<列出该类的全部方法（含构造/析构、含私有方法）。
"签名"一列写函数名与形参；
"调用方"一列写谁调用本函数——本模块内的类/函数，或跨模块的调用方；
"实现逻辑要点"用几句叙述讲清关键步骤、分支与边界条件；
"依赖的其他模块函数"一列写本函数内部调用了哪些**别的模块**的类/函数（本模块内部调用不写这里）。>

| 函数 | 签名 | 调用方 | 入参说明 | 返回值 | 异常 | 实现逻辑要点 | 依赖的其他模块函数 |
|------|------|--------|---------|--------|------|-------------|------------------|
| `<func>` | `<func>(<a>, <b>)` | `<模块>.<类>` 或 `<本类>.<函数>` | <每个参数的含义与约束> | <返回什么，什么情况下返回什么> | <抛出什么异常，什么条件下抛> | <关键步骤、分支、边界条件、并发处理> | `<模块>.<类>.<函数>()` |

## 4. 对外依赖契约

<本模块需要其他模块提供的类、函数、变量，写清**期望签名**与语义边界。
为什么写签名级：调用点写在**本模块**的代码里，函数名与入参/返回类型已经被本模块的实现逻辑锁死，
所以这些属于本模块有权规定的内容；提前钉死它们，能在实现前就发现接口不匹配。

本表是这些依赖签名的**唯一权威来源**——调用方定签名，提供方按此实现。
落盘时同步到架构总纲第 7 节，矩阵摘录必须与本表逐字一致。

**本表不记状态。** 这条依赖是否已被提供方满足，记在架构总纲第 7 节的接口矩阵里。
状态会因**别人**的落盘而变化，而本模块无权替别人翻牌；把状态收在一处，就不会出现两处对不上。>

| 提供方模块 | 需要的类 | 期望签名 | 语义与约束 | 需要的变量 / 字段 |
|-----------|---------|---------|-----------|------------------|
| <模块名> | `<ClassName>` | `<func>(<a>: <T>, <b>: <U>) -> <R>` | <入参含义与约束；返回值含义与边界（不存在 / 空 / 失败时返回什么）；幂等与并发要求> | `<var>` |

只需要引用对方的类、不需要具体函数时，「期望签名」写 `—`，并在「语义与约束」里说明这个类要满足什么（例如要能被本模块当参数类型使用、需要具备哪些字段）。

**硬约束（本模块定，提供方改动即产生差异）**

- 函数名、入参个数与顺序、每个入参的含义
- 返回类型，以及返回值的边界语义（不存在时返回什么、失败时抛什么或返回什么）
- 需要的变量 / 字段的存在、名称与语义（例如本模块要读取的常量）
- 行为约束：幂等性、线程安全、时序要求、性能底线

**软约束（提供方可调整，不产生差异）**

- 参数的具体类型定义、错误类型体系、内部数据结构

提供方调整软约束无需协商；调整硬约束则在架构总纲第 7 节置「有差异」，按落盘协议规则 3 列出差异交由用户裁决。

**填表规则**

1. 提供方尚未设计 → 在架构总纲第 7 节记 `待提供`；落盘时在变更清单中提示用户后续单独讨论该模块
2. 提供方已设计但硬约束与本模块期望不一致 → 在架构总纲第 7 节记 `有差异` 并列出差异，交由用户裁决。
   裁决结果需要改本表签名的，改本表；需要提供方改实现的，**不擅自修改对方的模块文档**，交给对方模块的下一次落盘
3. 提供方硬约束与本模块期望一致 → 由**提供方在自己落盘时**把架构总纲第 7 节的状态改 `已落地`（本模块不参与）
4. 本模块不需要任何外部依赖 → 保留本节标题，写"不适用：本模块不依赖其他模块"

本节只写需求，不写状态——状态一律看架构总纲第 7 节。

## 5. 端到端业务流程

<逐条业务流程展开，**本模块参与的每条流程都要写**。

流程名的对齐关系：**本文档第 5 节出现的每条功能流程**，流程名与架构总纲第 10 节保持同名——
架构总纲第 10 节画的是**方向级骨架**，并登记**全部功能流程**，包括模块级调用链只涉及一个模块、
只在本模块内就走完的功能流程。

分工：架构总纲第 10 节画的是**方向级骨架**（模块级调用链 + 每跳承担的核心类 + 每跳的数据形态），
本节写**本模块参与的每条流程**的函数级细节——本模块怎么被进入、内部哪个函数调哪个函数、最后交给谁。
流程跨模块也好、只在本模块内完成也好，本节都按架构总纲第 10 节的登记名展开；
跨模块的那一跳只需写明"调用 <模块> 的 <接口>"，方向级骨架本身不在这里重复。

边界：只有**纯模块内部的实现级流程**——本身不是功能流程、只是把某条流程在本模块内的实现步骤再拆细——
才只写在本节、不要求登记到架构总纲第 10 节。**只在一个模块内完成的功能流程不属于这一类**：
它仍是功能流程，必须在架构总纲第 10 节登记，并按同一流程名在本节展开。

每条流程讲清六件事，缺一不可：
服务功能点（这条流程服务于架构总纲第 5 节的哪个或哪些功能点）
→ 触发入口（本模块被谁、在什么条件下进入）→ 本模块内的函数级调用链
→ 每跳的数据形态（传进去什么结构、变了什么、传出来什么结构）
→ 结果投递给谁（返回值给上游、调用下游哪个接口、事件发给谁、写到哪）
→ 异常路径（哪一步可能失败、失败后如何回滚或降级、错误抛给谁）。
下面的骨架是对单条流程的填写格式，按流程复制。>

### 5.N <流程名>

**服务功能点**

<这条流程服务于架构总纲第 5 节的哪个或哪些功能点，并与本文档第 1.2 节「本模块参与的功能点」逐条对应。
**在架构总纲第 10 节登记的每条流程**（**跨模块的**与**只在本模块内完成的**都算），其服务功能点必须与架构总纲第 10 节同名流程登记的服务功能点一致——两处对不上就是缺陷。>

**触发入口**

<本模块是被谁、在什么条件下进入的：上游调用 / 定时任务 / 事件订阅，写明具体触发点。>

**调用链**

<列出**本模块内部**的函数级调用链。
例：`<本模块>.<类>.<函数>()` → `<本模块>.<类>.<函数>()`。
需要调用其他模块时，写成"调用 `<模块>.<类>.<函数>()`"，并注明骨架见架构总纲第 10 节。
流程本身绕或多分支时，可另加 mermaid 时序图辅助阅读；图不替代表格。>

**数据形态变化**

| 步骤 | 所在函数 | 输入数据形态 | 输出数据形态 |
|------|---------|-------------|-------------|
| 1 | `<本模块>.<类>.<函数>` | <结构/字段/类型> | <结构/字段/类型> |

**结果投递**

<本模块把结果交给谁：返回给上游哪个调用方、调用下游哪个接口、事件发布到哪个通道、
数据落到哪个存储、状态变更推送给哪个下游。写明投递的形式与时机。>

**异常路径**

<本模块链路上哪一步会失败、失败的表现、失败后的处理（重试 / 回滚 / 降级 / 上抛），
以及错误最终由谁感知。>

## 6. 关键决策记录

<仅记录本模块层面的取舍。架构层面的决策写在 `01-架构设计.md` 第 11 节。>

| 决策点 | 选择 | 理由 | 被否决的选项 |
|--------|------|------|--------------|
| ... | ... | ... | ... |

## 7. 待定问题与风险

<待定项解决后、风险解除后，可以删掉对应条目——删条目属于「受影响条目」，不需要额外授权。
新增的待定项与风险写在这里。>

- **待定**：<还没想清楚的问题> —— 为什么未定：<...>
- **风险**：<可能出问题的地方> —— 影响：<...>
